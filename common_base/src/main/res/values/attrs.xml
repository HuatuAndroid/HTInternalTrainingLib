<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--话题编辑-->
    <declare-styleable name="ContentEditor">
        <attr name="ce_text_padding" format="dimension|reference" />
        <attr name="ce_text_lineSpacingExtra" format="dimension|reference" />
        <attr name="ce_text_color" format="color|reference" />
        <attr name="ce_text_size" format="dimension|reference" />
        <attr name="ce_img_padding" format="dimension|reference" />
        <attr name="ce_img_txt_spacing" format="dimension|reference" />
        <attr name="ce_img_img_spacing" format="dimension|reference" />
        <attr name="ce_hint" format="string|reference" />
    </declare-styleable>

    <declare-styleable name="SkinLayoutPosHelper">
        <attr name="android:layout_marginLeft"/>
        <attr name="android:layout_marginTop"/>
        <attr name="android:layout_centerHorizontal"/>
    </declare-styleable>

    <declare-styleable name="ExCirclePageIndicator">
        <attr name="space" format="dimension|reference" />
        <attr name="fillRadius" format="dimension|reference" />
        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered" />
        <!-- Color of the filled circle that represents the current page. -->
        <attr name="fillColor" />
        <!-- Color of the filled circles that represents pages. -->
        <attr name="pageColor" />
        <!-- Orientation of the indicator. -->
        <attr name="android:orientation" />
        <!-- Radius of the circles. This is also the spacing between circles. -->
        <attr name="radius" />
        <!-- Whether or not the selected indicator snaps to the circles. -->
        <attr name="snap" />
        <!-- Color of the open circles. -->
        <attr name="strokeColor" />
        <!-- Width of the stroke used to draw the circles. -->
        <attr name="strokeWidth" />
        <!-- View background -->
        <attr name="android:background" />
    </declare-styleable>

</resources>
